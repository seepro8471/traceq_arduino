# 4차 전체 로직 감사 (2026-09-25 ~ 26) → v2.2.10

사장님 지시: "상수 변수초기화, 처리속도, 재귀호출, 중복코드, 콜백, 필드 호출의 일관성, 불필요한 재계산,
메모리 누수, 자원 해제, 안정성, 스레드관리, 태그기록의 안전성 — 전체 로직을 차분히, 하위 에이전트와 분담해서."
8갈래(A 태그 안전 · B RFID 계층 · C 초기화/설정 · D 처리속도 · E 중복/콜백/재귀 · F 메모리/스택 · G 안정성 · H 시험 장치)
+ 내 수정 재추적 검토 1회.

## 뿌리별 결과

| 뿌리 | 결함 | 고침 |
|---|---|---|
| 1 같은 태그 재처리 | 리더 재초기화가 present 를 지움(칩 리셋=안테나 OFF 로 정지도 풀림) → 세척 3초 뒤 '종료' · 비지원 카드(sak≠1K/4K) 정지 안 함 → 그 뒤 스코프 무음 | `Reinitialize` 3줄 삭제 · `Poll` Invalid 에 `HaltA` |
| 2 게이트웨이 입력 | 검사일시만 초기화, 환자 형제 잔존 → 남의 환자+검사일시 0 · 버퍼에 여러 레코드면 첫(옛) 환자 | 한 벌 통째 초기화 · **마지막 G1 레코드로 좁힌 뒤 첫 일치**(재추적 검토가 내 1차 수정 "마커별 마지막 일치" 의 필드 혼합을 잡음) |
| 3 소거 반환값 무시 | 발급 중 Clear 3곳 → 지난 환자·완료상태 품은 '새 태그' | 반환 검사 후 break |
| 4 커밋 순서 | 서버 소거가 커밋 앞 → 2차 덤프가 빈 칸으로 PC 덮음 · 자동 종료가 커밋 뒤 → 실패해도 성공음 · 종료 기록 시각 먼저 → 옛 담당자+새 시각 · 소독기 이동만 커밋 먼저 | **사장님 09-25: 커밋 먼저 → 소거 뒤**(PC 기록 보호, 재접촉 복구 포기) · 자동 종료 커밋 앞 · 시각 마지막 · 이동은 종료 기록 뒤 커밋 |
| 5 서버 연결 확인 | `legacy_is_connected` 1B/10ms 가 PC 명령을 먹고 55B 넘으면 'Not Connected' | **미수정** — 발급 재위험과 함께 별도 회차(사장님 "그대로 고친다" 판정은 받음, 이번엔 P1 우선) |
| 6 설정 JSON | 알 수 없는 타입 → 매번 재시작 · 없는 키 0 저장 | 저장값으로 재시작 판정 · `containsKey` 8곳 |
| 7 표시/성능 | 홈 1행·R-O 매 루프 재기록(루프 56ms) · 서식 문자열 RAM | 캐시(`InvalidateHome` 이 함께 지움) · `snprintf_P` |
| 메뉴 | 무조작 상시 대기(태그 못 읽음·버튼 고장 시 탈출 불가) | **사장님 09-25: 60초 무조작 → 저장 없이 홈**(Exit 경로, 소프트 리셋 없음) |

깨끗함: 누수·스택 넘침·오버런 재현 0(최악 스택 여유 3.7KB) · 제품 함수 재귀 0(ELF 호출 그래프) · 콜백 0 · 경고 0.

## 시험

- `t_a4`(15) 12자리 · `t_lcd`(6) 캐시/무효화 · `t_menu`(7) 시한 — **각 수정 되돌린 변이 전부 빨강**(11/11 · 2/2 · 2/2).
- 장치: `nackBlock`(카드는 살리고 블록만 NACK — 순서·커밋 결함을 가른다, 가정 ③) · `buttons_at`(시각 지정 버튼) ·
  `g_btnIdleLimit` · `g_versionReg`/`sim_field_drop`(죽은 리더·안테나 OFF) · runall 거짓 초록 3경로 차단(멈춤·요약 없음·CHECK 0) ·
  build.sh 관문 줄 보존.
- 시험의 "동기된 시계" 표본은 `rel_date()`/`REL_YMD` 로 출시일에서 유도 — 버전 올릴 때 깨지던 것.

## 교훈

- **내 봉합이 새 결함을 만들었다**(마커별 마지막 일치 → 레코드 혼합, `G3PAT` 잘림). 재추적 검토가 잡았다 → 내 diff 는 항상 독립 재추적.
- **잠금은 "지우면 빨강" 까지** — 첫 t_a4 는 11 중 4 만 물었다(순번 주입이 카드를 죽여 순서 차이를 못 봄 → 블록 주입).
- **발급 명령 `S99;` 는 세미콜론 둘**(`S번호;시리얼;`) — 하나면 파싱에서 죽어 시험이 Clear 에 닿지 않는다.
- 시험 장치의 멈춤이 초록으로 읽히면 "시한" 류 잠금은 전부 헛것 — runall 이 먼저 정직해야 한다.

## 후속 (09-26 오후) → v2.2.11

"다음 회차" 목록 중 코드로 닫히는 것 전부 처리. 독립 재추적 검토: **이상 없음**(성공 경로 바이트·인증 횟수·시리얼 출력 HEAD 와 동일, peek 22경우, 실물 ELF RAM 동일).

| 항목 | 처리 |
|---|---|
| 뿌리 5 서버 연결 확인 | `peek` — 'Z' 아니면 곧장 거부(명령 유실 0, serialEvent 가 처리, 재접촉 성공). 세 PC 모두 'Z' 한 바이트임을 소스로 확인 |
| E P2-1 담당자 정본 우회 | 소독기 4줄 → `write_manager_key/name`. 잠금: 세척·소독 8블록 동일(담당자 ID 14자 전부 채워 마지막 바이트까지 본다) |
| E P2-2 WriteBlocks 검증 무잠금 | `writeVerified` 하나로 통합(플래시 −110B). 잠금: 검사명 3블록 확인 읽기 실패 → Status 0·Sm! 없음 |
| E P2-3 print_tag_number 이름 | 이름은 안 바꿈(리팩터 금지). **행위**를 잠금: 번호 읽기 실패 시 직전 스코프 번호가 PC 로 안 나감 |
| 헛초록 M45/M46 | 잠금: G2 없는 패킷은 이전 검사일시 이월 금지 · guest 판정은 대입(이틀 뒤 단독 소독 = host, 횟수 +1) |
| t_notify 서버 훑기 CHECK(true) 4 | 커밋 위치(off=3)·커밋 뒤 상태·재접촉 2차 덤프 유무를 회차마다 실제로 본다 |
| 첫 부팅 소거 13.7초 | "Initializing… %" 진행 표시(lcd.init 중복은 무해 — 라이브러리 begin) |

교훈: 시험 픽스처의 `Status=1` 이 "안 섬" 판정을 무력화했다(픽스처가 결론을 미리 정해 둔다) · 시뮬은 `serialEvent` 를 아두이노 코어처럼 시험이 직접 불러야 한다 · 변이가 안 보이면 **데이터가 변이를 가리는지**(ID 7자 뒤 0)부터.

## P3 마무리 (09-26 밤) → v2.2.12

값·표시가 조용히 틀리던 P3 네 자리. 잠금 `t_a6`(6) · 변이 5/5 빨강 · 261/261.

| 항목 | 무엇이 틀렸나 | 고침 |
|---|---|---|
| C-3 | 소독 시작의 `SECTOR14` 읽기는 **값을 한 바이트도 쓰지 않는데**(10바이트를 아래에서 전부 덮어쓴다) 실패하면 시작을 취소했다. 그 앞에서 이미 옛 기록을 지워 **"옛 기록 없음 + 새 시작 없음"** 태그가 남았다 | 읽기 제거(성공 경로 바이트 동일) |
| C-4 | 게터가 **세터가 저장하지 않는 값**을 돌려줬다. 세척시간이 음수면 자동 종료가 **시작보다 앞선 시각**으로 태그에 찍힌다 | 게터에 세터와 같은 범위(0~127·0~2·0~120·음수 0) |
| G P2-3 | 알람 남은 시간을 하루 안 시각끼리 빼서 **자정 넘김에 음수**, `minutes()` 가 `(초/60)%60` 이라 **60분 초과 설정에서 나머지만** | `GetAlarmRemainingSeconds`(절대 시각 차, 0 클램프) |
| H P3-H4 | 폐기 블록(18·52) 소거 실패를 무시하고 그 블록을 **읽어서** 덤프 → 옛 바이트가 PC 로 가고 알림도 없음 | 소거 실패면 읽지 않고 0 줄(`legacy_print_block(..., zeroOnly)`) |

교훈: **내 시험 셋이 또 틀렸다** — EEPROM 주소를 짐작해 헛통과(세터를 안 거치는 시험은 주소를 소스에서 확인) · 고장 주입이 쓰기 검증까지 막아 판정 불가(`readErrSkip` 로 첫 읽기만 통과) · 1행 캐시 때문에 화면 로그가 비어 `InvalidateHome()` 필요 · **음수 검사에 `lcd_has("-")` 는 `R-O` 를 잡는다**(부정 검사는 무엇에 걸리는지부터).

## 남은 것 (다음 회차)

- (코드로 닫히는 것은 09-26 후속에서 전부 처리 — 위 표)
- 현장 확인: ① 리더 재초기화 재현(B P2-1 모델은 데이터시트 근거) ② 가정 ④(올눈 조각 타이밍).
