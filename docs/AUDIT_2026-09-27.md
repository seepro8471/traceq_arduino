# 5차 전체 로직 추적 감사 (실제 날짜 2026-09-26 — 파일명의 09-27 은 내 시계 착오, 출시일도 27 로 잘못 올렸다가 재검증에서 잡힘) → v2.2.13

사장님 물음: "왜 검증해서 수정해도 또 검증에서 걸리는가." 답(이번 세션 기록): ① 내 수정이 다음 결함의 최대 공급원(호출자·형제를 같이 안 봄)
② 시험이 못 잡음(첫 잠금 11 중 4 · 픽스처가 결론을 미리 정함 · 주소 짐작) ③ P3 로 미뤄 둔 것이 실제 결함 ④ 주제로 나눠 같은 함수를 반씩 봄.

이번 회차의 다른 점: **모듈 6갈래가 함수 219개(7,516줄)를 줄 단위 표**로(루프 탈출·if 진리표·변수 초기화·반환→소비·중복·공유 상태) · P3 도
전부 기록 · 보고 규칙 파일(`scratchpad\fw5\SCOPE.md`: 실패 시나리오 없으면 결함 아님 · 주석 근거 금지 · `자신 없는 것` 필수) ·
**종료 조건 = 독립 재추적 P1/P2 0 + 안 고친 것은 코드에 `[5차 판정 · 재론 금지]`**.

## 결과: P1 2 · P2 12 · P3 68 → 제품 수정 30자리 · 재론 금지 6 · 시험 t_a7(24)

| 뿌리 | 결함 | 처리 |
|---|---|---|
| 내 앞 수정의 후속 영향 | 붙은 버튼이 시한을 미룸(A2) · Invalid 카드 present 잔존 → 스코프 KeepAlive(B) · Write Error 뒤 재접촉이 4초 종료(C P1-2) | 에지 활동 · present 초기화 · **write_process 재읽기**(커밋됐으면 성공) |
| 동시소독 슬롯 | 슬롯 없는 스코프 종료가 host 슬롯 소거 → 다음이 host(C P1-1) · 확인 실패 시 횟수 0 | `else if (is_host)` · 재읽기 |
| 시계 | 날짜·시간 저장이 진입 시각(A2·E 독립) | `FromInternalString` 이 지금 시각 |
| 게이트웨이 파싱 | 값 속 `G1234`/`KIG4M`/`EG5D` 로 환자정보 유실(D) · 월 13 → RTC 2027 | `find_marker`(맨 앞·';'·G1/G5 뒤) · G2 범위 |
| 커밋 플래그 | Status=1 태그 재등록 실패 → Status=1+혼합(D) | 먼저 0 으로 내리고 옛 환자 블록 소거 |
| 발급 | 번호 -1(D) · 도중 전원 → 영구 불가(B) | `<0 → -1` · 접근비트 이미 TraceQ 면 skip |
| 담당자 일회성 | 실패한 시작·종료 터치가 소모(C) | 커밋된 시작 뒤에만 `consume_disposability()` |
| 사장님 판정 3 | 짧은 종료 **그대로** · 무음 태그 **거부음 통일** · 유지+빈 교환일 **1개월 전** | 재론 금지 · `RejectDebug` · `OneMonthBefore` |
| P3(값·화면) | 제목 괄호 · 알람 줄 20자 · `<>` 동시 · 선택 첫 항목(타입 D→G 재시작!) · 슬롯 3 · 교환일 빔 판정 · 문자열 경계 2 · 담당자 NUL · 손상 연도 · 재인증 낭비 · millis 랩 | 전부 고침 |

재론 금지(코드): 더블터치 2초(RecordProcessor) · 이동 플래그 수명(DisinfectionProcessor) · ClearSector(5)(6) · 알람 0분(AlarmOption) · 둘째 JSON(SerialProcessor) · 폴백 Status 먼저(GatewayProcessor) · 출시일은 과거로(version.hpp).

## 검증
- 되돌린 변이 **22/22 빨강**(`scratchpad\fw5\mut_a7.py` + x_g5·x_clr) · 3차 시험 둘("커밋됐지만 Write Error")은 그 결함을 정상으로 박아 뒀던 것 → 뒤집음.
- **독립 재추적**(`AUDIT_final.md`): P1/P2 0 · 성공 경로 34단계 카드·시리얼 지문 HEAD 동일 · 와이어 문자열 집합 동일 · P3 5 전부 반영.
- 세 env 빌드 · RAM 2999B · 16종 283 CHECK.

## 교훈
- **Bash 도구는 백슬래시를 두 번 먹는다** — `\n`·`\0` 이 실제 바이트가 됐다(NUL 이 소스에 들어감). 한글·백슬래시 든 편집은 Write/Edit 로.
- **변이 스크립트의 `replace(…,1)`** 이 4자리 중 1자리만 되돌려 헛"놓침"(e4) — 같은 문장이 여럿이면 전부.
- 픽스처가 실물과 다르면(트레일러 접근비트 0) 새 시험이 헛빨강·헛초록 — 가짜는 **실물 바이트**로.
- 재론 금지는 보고서가 아니라 **코드에** — 4차 P3 가 5차에 결함으로 돌아온 이유.

## P3 마무리 (09-27 밤) → v2.2.14

사장님 "남는 것 없이 다 작업한 거야?" — 남아 있었다: P3 68 중 처리 안 된 43. 전수 처분:
- **고침 11**: 번호 ≥100 표시 · 미인증 서버 거부음 · MaxCount 가림 · 폴백 송신 시점 · device_type "" · 파싱 먼저 · ClearSector 범위 ·
  Screen 0줄 · 헤더/주석 3.
- **재론 금지 31** — 전부 해당 자리에 `[5차 판정 · 재론 금지]` 주석(보고서에만 남기면 다음 회차에 결함으로 돌아온다).
- **되돌림 1**: Invalid Date 를 800ms 로 늘리려다 `Notify(800)` = 긴 삐 한 번 → 사장님 소리 규칙(실패=짧게 4회) 위반 → 원복·재론 금지.
- 시험 t_a7 +6 · 변이 6/6 빨강 · 288/288. 교훈: **표시 시간이 소리 길이에 묶인 함수는 소리 규칙부터 본다**.

## 재검증 (09-26 밤) → v2.2.15 — "다시 검증에 걸린 것 없게"

4갈래(V1 diff 재추적 · V2 모듈 관계 · V3 전체 재통과 A · V4 전체 재통과 B). 결과 **P1 2 · P2 3 · P3 12** → 전부 처리.

| 갈래 | 걸린 것 | 처리 |
|---|---|---|
| V1 | **출시일 27, 빌드 날 26**(내 실수 — 내가 쓴 재론 금지 규칙을 내가 어김) · col15 잔상(내 4차 수정의 반대 방향) · 999 상한 · Scope 5자리 · static_assert 거짓 인용 · doxygen 깨짐 | 26 + **`__DATE__` static_assert**(미래면 빌드 실패) · 15열 5칸 고정 · 999 · [14] · Process.hpp static_assert · 복구 |
| V2 | **이동 플래그가 Read Error 에 소모**(담당자 일회성의 형제 — 5차가 반만 고침) · 레거시 발급이 검사일시·검사명 남김 · 알람 절대 시각 | 거부에만 소모 · Clear 2 · 재론 금지 |
| V3 | **홈 SELECT 0.6초 이상 → 기기번호 편집 직행**(4차 A1 자신 없던 것 재현) · 자정 낀 시간 저장 +24h(내 E1 수정의 새 경계) · 177 손상값 같은 판 미정리 · MaxCount 제목 · 재론 금지 전제 오류 2 | 손 뗄 때까지 대기 · ±1일 보정 · 매 부팅 정리 · [18] · 문구 정정 |
| V4 | **더블터치 판정 읽기 실패 → 1초짜리 종료**(내 재론 금지가 안 다룬 갈래) · 문구 오분류 | 3진값·Read Error·불변 · 재론 금지 |

V2 새 사실(사장님 판정 대상): 3차 P3 "2폴 안 카드 교체 갇힘" 의 전제 "2폴≈100ms" 는 틀림 — 처리 loop 안에 알림음이 통째로 들어 **최대 1.6초**. 그 사이 스코프를 바꿔 올리면 다음 태그가 KeepAlive 로 무음(떼었다 다시 대면 처리, 잘못된 기록 없음). 고치려면 "마지막 처리 UID" 상태 하나가 는다.
V2 자신 없는 것(설계 판정): 게이트웨이 관문이 "세척만 하고 소독 안 한 스코프(WS=1·DS=0)" 를 통과시킨다(1.0 동일).

교훈: **내가 쓴 규칙을 내가 어겼다**(출시일) → 사람 확인이 아니라 컴파일러가 막게 · **형제를 반만 고쳤다**(플래그 둘 중 하나) · **내 수정이 새 경계를 만들었다**(E1→자정 · col15→잔상) · 재론 금지 주석의 전제도 검증 대상.

## 재검증 2회차 (09-26 밤) → v2.2.17

W1(v2.2.15~16 diff 재추적)·W2(30일 장기 시뮬·퍼징 100회·동시 입력·손놀림·경고 전수·재론 금지 전제) → **P1 1 · P3 4**.
- **P1(두 갈래 독립 발견)**: 내 V3 자정 보정("지금과 12h 넘게 차이면 하루 옮김")이 방전 시계를 처음 맞추는 조작을 전날로 보냄 → "편집 중 날짜가 바뀐 경우에만 진입 날짜/지금 날짜 중 가까운 쪽". **내 수정이 만든 결함을 재검증이 잡았다 — 두 번째**.
- P3: is_valid 3진값(이동 플래그는 거부에만) · 본체번호 4자리 표시 · 주석 전제 2 · 창 밖 Reinitialize 재처리 재론 금지.
- W2 문제없음(근거): 30일 시뮬 누적 어긋남 0(윤년·연말·millis 랩 포함) · 퍼징 300회 리셋 0·기록 오염 0 · 동시 입력 섞임 0 · 손놀림 조합 18/18 · 경고 집합 2.2.14 와 동일 · **게이트웨이 WS/DS 거부는 세 PC 절차와 충돌 없음**(정상 주기 G→W→D→S 뿐).
- 변이 3/3 빨강 · 311/311(17종 · t_a8 분리).

## 재검증 3회차 (09-26 밤) → v2.2.18

X1(v2.2.17 diff 재추적 · 자정 보정 조합표 29)·X2(새 각도 전체 재통과 · 두 태그 모델) → **제품 P1 0 · P2 0 · P3 1 · 판정 2 · 내 잠금 구멍 3**.
- P3: `IncrementCount` 32767+1 이 int16 을 돌아 0 → "MaxCount Over" 풀림 → 상한에서 멈춤(잠금 · 변이 빨강).
- 판정(재론 금지 주석 + 계약 시험): ① **동기된 소독기도 세척기가 세척 시간 넘게 앞서면 따라간다**(X2 P2 후보 — 1.0 동일 · 세척기·소독기는 설정기로 맞춘 뒤 단독 운용이라 세척기가 유일한 운용 중 시각 기준 · 게이트웨이←SeePro 와 같은 규칙 · `IsUnsynced` 로 막으면 뒤처진 소독기가 영영 못 따라감 · 되돌리면 시험 빨강) ② 두 태그 **겹침**+HaltA 유실 → 앞 태그 재처리(두 조건 동시 · 2칸 이력은 상태 증가라 두지 않음 · 겹침은 현장 목록 ④) ③ 자정 편집 중 큰 보정은 같은 입력에 정답이 둘.
- 내 잠금 구멍 3(X1): 자정 넘긴 편집에 **새 날 시각** 갈래 · is_valid ③(Process 읽기 실패)의 이동 플래그 · 죽은 `buttons_script("LS")` → t_a8 보강, 놓친 변이 2 가 빨강으로.
- 변이 4/4 빨강 · 316/316(17종).

## 재검증 4회차 (09-27) → v2.2.19

Y1(v2.2.18 diff 재추적 · 재론 금지 전제를 PC 소스로 검증)·Y2(정수 상태 전수 + "어느 줄에서 리셋이 나도 다음 부팅이 안전한가" 실측) → **제품 P1 0 · P2 0 · P3 4 · 판정 1 · 내 잠금 구멍 1**.
- **뿌리 하나: 연속된 EEPROM 쓰기에 원자성이 없다**(바이트당 3.3ms · 창 수십 ms · 1.0 동일). 절단점 n 을 0..K 전부 훑어 남는 상태를 실측했다.
  - 클리어 태그: 교환일 8바이트가 찢겨 **옛 일 + 새 월**(2026-09-15)이 남고 그 뒤 소독 기록·태그 상세(56)에 실렸다 → **미룸을 먼저 세운다**(절단되면 다음 부팅이 다시 쓴다).
  - 담당자 등록: 표지를 먼저 올려 **반쪽 담당자**(새 키 + 옛 이름)가 '등록됨' 으로 태그 21/22 에 실렸다 → `SetKey`/`SetName` → **`SetData` 하나**, 표지는 바이트를 다 넣은 뒤 한 번만(절단되면 '담당자 없음' → 다시 대면 복구).
  - 같은 판 RIGHT 소거 중 리셋: 도장이 그대로라 다음 부팅이 **묻지도 않고** 반쯤 지워진 설정(타입 W·번호 0·담당자 없음)으로 기동 → 소거 loop 앞에서 도장 무효(1줄).
  - [판정·재론 금지] 미룸 해소 전 절단은 교환일이 재부팅 시각 — 순서를 바꾸면 날짜가 아예 안 남아 이쪽이 최선.
- Y1 제품 결함 0. 새 재론 금지 주석 셋의 전제를 **PC 소스로 확인**: 세척관리·설정기·델파이 전수에서 W·D 리더의 시계를 바꾸는 길은 설정기(수동)·자기 메뉴·세척기 태그(앞으로만)뿐 → "설정기로 맞춘 뒤 단독 운용" 이 사실. 두 태그 겹침 재처리도 "두 조건 동시" 가 사실(한 장 경로 4 전수).
- 내 잠금 구멍 1: 포화 CHECK 가 32767 만 물어 `< INT16_MAX-1` 변이가 초록 → 경계 32766→32767 CHECK 추가.
- 정수 상태 전수(EEPROM 13·RAM 카운터 20·시각 산술 16·snprintf 12)에 새 결함 0 — 세터 범위 = 게터 클램프 실측, millis 뺄셈형 8곳 정상, 실제 랩은 `IncrementClearCount`(JSON 통계만) 하나.
- 시험 장치 **전원 차단 주입**(`g_eepromCutAfter`/`power_restore`)을 제품 시뮬로 이관 · 새 `t_a9`(10 · 절단점 전수) · 변이 7/7 빨강 · **327/327**(18종) · 실빌드 RAM 3020B · Flash 52150B.

## 재검증 5회차 (09-27) → v2.2.20

Z1(Y 봉합 재추적)·Z2(긴 조작 중 시리얼 경합 + 현장 항목) → **P1 0 · P2 1 · P3 7 · 기각 1**.
- **내 v2.2.19 봉합이 만든 회귀 둘**(Z1): ① `SetData` 가 표지를 먼저 내리자 "표지 거짓 + 칸에 반쪽" 상태가 생겼는데 `load_manager_data` 가 표지를 안 봐 **종료 담당자 블록(16/17·25/26)에 반쪽 담당자가 기록**됐다(재론 금지 ⑫ 의 전제도 내가 깼다) → 읽는 쪽이 `HasData()` 를 먼저 본다. ② 같은 담당자 재접촉마다 표지가 1→0→1 로 **2회 닳았다**(일회성 ON·하루 40건이면 약 5년) → 바뀔 때만 쓴다.
- P3(Z1): 미룸을 **횟수보다도 먼저**(횟수만 써지고 끊기면 액교환이 기록에서 사라지고 MaxCount Over 도 안 떠 다시 댈 이유가 없었다) · 기본값 기록의 유효 표지를 **타입 하나로**(`DeviceOption::Upload` 은 타입을 마지막에 · `deviceOption.Upload()` 를 전체 마지막으로 → 소거 절단점 56/56 깨끗) · 부팅 교환일 두 자리도 미룸 먼저.
- **P2(Z2) 게이트웨이 수신 관문**: RX 링은 511바이트에서 **새로 온 바이트를 조용히 버리고**, `readBytes(…,511)` 의 나머지는 다음 버퍼의 머리가 된다. 그 꼬리를 받아들여 검사항목을 통째로 빈칸으로 기록하거나(성공음) 검사일시 0 으로 기록했다 → 레코드는 **머리(G1·G2) 경계에서 시작하고 G5 로 끝나야** 쓴다. G1 이 없는 버퍼는 마지막 `G2` 를 레코드 시작으로(종전엔 **첫(옛) 환자**를 골랐다).
- P3(Z2, 사장님 허가 09-27): 511 에서 끊긴 전문의 꼬리를 명령으로 읽어 값 한가운데의 `C`/`M`/`S` 가 발급으로 실행돼 **리더 위 태그가 덮였다** → 앞 읽기가 한도에서 끊겼으면 그 버퍼는 버린다(와이어 형식 불변).
- **기각 1**: Z2 의 P1("올눈은 G1 을 안 보내 병합 시 첫 환자가 기록된다")은 전제가 틀렸다 — `MainFormSo.pas` 는 32,551줄로 인용된 `:58983`·`:59014` 가 없고, `UserInfoReaderSet`(:15176)은 **:15207 에서 G1 을 실제로 보낸다**(G2~G5 와 같은 `SendBtnClick`). G1 없는 버퍼는 우리 시험의 올눈 모델과 511 절단에서만 생긴다.
- **내 잠금 구멍 5**: t_a9 의 CHECK 셋이 되돌림에도 초록(하나는 동어반복)·재개 시각을 초 단위로 비교해 `delay(700)` 하나에 헛빨강·소거 CHECK 가 "묻기만 하면 안전"으로 셈 · 로그 버퍼가 차서 뒷 절단점이 안 찍힘 · G5 관문을 버퍼 전체에서 찾는 변이가 통과 → 전부 행위로 고쳤다.
- 변이 **17/17 빨강**(무해 5 초록) · **354/354**(19종) · 실빌드 RAM 3020B · Flash 52442B.

## 현장 확인 목록 — 7 중 4 를 시뮬로 닫았다(v2.2.20)
닫힘(현장 불필요 · `t_a10` 25 CHECK · 변이 4/4 빨강): **③ 첫 부팅 진행 표시 뒤 LCD 잔상**(20x4 표시 격자로 80칸 전수 — 부분 문자열 검사는 변이가 통과해 헛초록이었다) · **④ 출입카드+스코프 동시 인식**(두 장 카드 모델 — 어느 쪽이 먼저 뽑혀도 스코프는 정상 처리 · 얹어 둔 출입카드의 실패음은 1회로 끝난다) · **⑤ 반만 바뀐 태그 재발급**(공정·환자·검사일시·검사항목 전부 0 으로 복구 = 멱등) · **⑦ 홈 SELECT 길게 누름**(2초 상한을 넘겨 눌린 채여도 멈추지 않고 60초 시한으로 홈 · 같은 loop 이 올려 둔 태그를 처리).
시험 장치 이관: 두 장 카드 모델(X2)·LCD 20x4 표시 격자(Z2) — 기존 18종 로그가 이관 전후 동일.

## 남은 것 — 현장만
① 리더 재초기화 재현 ② 올눈 조각 타이밍(1초 넘는 틈 — 1초보다 짧은 쪽은 델파이 소스로 확정) ⑥ 일회성 담당자가 종료 터치엔 소모되지 않게 된 것(1.0 과 다름 — **현장이 아니라 사장님 판정 사항**). ⑦ 의 실기 버튼 접점 바운스만 현장.
사장님 판정 2 (09-26) → v2.2.16: ① 알림음 중 카드 교체도 **처리**(마지막 처리 UID 로 "1회만" 판정 — 3차 P3 전제 100ms 는 틀림) ② 게이트웨이는 WS 또는 DS 가 선 스코프를 **거부**("No Complete" 그대로). 변이 2/2 빨강 · 305/305.
